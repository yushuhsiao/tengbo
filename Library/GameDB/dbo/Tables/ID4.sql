CREATE TABLE [dbo].[ID4] (
    [I] INT              IDENTITY (1, 1) NOT NULL,
    [G] UNIQUEIDENTIFIER NOT NULL,
    CONSTRAINT [PK_ID4] PRIMARY KEY CLUSTERED ([I] ASC),
    CONSTRAINT [IX_ID4] UNIQUE NONCLUSTERED ([G] ASC)
);

