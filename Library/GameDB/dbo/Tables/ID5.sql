CREATE TABLE [dbo].[ID5] (
    [I] INT              IDENTITY (1, 1) NOT NULL,
    [G] UNIQUEIDENTIFIER NOT NULL,
    CONSTRAINT [PK_ID5] PRIMARY KEY CLUSTERED ([I] ASC),
    CONSTRAINT [IX_ID5] UNIQUE NONCLUSTERED ([G] ASC)
);

