CREATE TABLE [dbo].[ID2] (
    [I] INT              IDENTITY (1, 1) NOT NULL,
    [G] UNIQUEIDENTIFIER NOT NULL,
    [S] VARCHAR (16)     NOT NULL,
    CONSTRAINT [PK_ID2] PRIMARY KEY CLUSTERED ([I] ASC),
    CONSTRAINT [IX_ID2] UNIQUE NONCLUSTERED ([G] ASC)
);

